# Automation Notes

This toolkit generates upload-ready JSON files and bundle manifests.

You can pair it with any manual upload flow or browser automation you already use.

Recommended flow:

1. Run `python generate_outputs.py ...`
2. Upload the generated JSON files to your target web service
3. Use your preferred automation or manual process to submit runs and collect results
